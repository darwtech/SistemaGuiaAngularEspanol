export interface casa {
  id: number;
  nombre: string;
  ciudad: string;
  provincia: string;
  foto: string[];  // Cambiado a un arreglo de strings
  unidades: number;
  wifi: boolean;
  lavanderia: boolean;
}
